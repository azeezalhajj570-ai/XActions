# public/

Static assets served at the site root: logo, icons, Open Graph images, `manifest.json`, and the demo video.

They are copied into the site build (`npm run site:build`) and served by the API in production. Add new OG images here and reference them from the page `<head>`.
